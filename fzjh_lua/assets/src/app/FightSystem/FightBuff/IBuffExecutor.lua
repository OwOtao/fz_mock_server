local interface = require("third.class.interface")

local IBuffExecutor = {}

function IBuffExecutor:attackExecute(attackBuffExecutors)
end

return interface("interface", IBuffExecutor)
00000000000000