local interface = require("third.class.interface")

local IRolePresenterInput = {}

function IRolePresenterInput:useItem(itemId)
end

return interface("IRolePresenterInput", IRolePresenterInput)
00000