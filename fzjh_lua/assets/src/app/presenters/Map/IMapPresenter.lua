local interface = require("third.class.interface")

local IMapPresenter = {}

function IMapPresenter:setInput(input)
end

function IMapPresenter:setOutput(output)
end

return interface("IMapPresenter", IMapPresenter)00000000000000