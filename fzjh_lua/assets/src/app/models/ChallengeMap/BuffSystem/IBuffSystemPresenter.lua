local interface = require("third.class.interface")

local IBuffSystemPresenter = {}

function IBuffSystemPresenter:showAddBuff(printText)
end

function IBuffSystemPresenter:showRemoveBuff(printText)
end

return interface("IBuffSystemPresenter", IBuffSystemPresenter)
0000000000